Sovereign Serve 1.0.3 GitHub Build Fix

Replace these files in the repository:
1. sovereign-source.zip (repository root)
2. .github/workflows/build-android.yml

The source package corrects the native PDF compiler error, removes the duplicate launcher background resource, removes the environment-specific npm lockfile, and updates the app to version 1.0.3 (103).
